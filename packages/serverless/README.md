# serverless

## install project:

```
yarn
```

To set up serverless:

```
sls create -t serverless.yml
```

To run the API:

```
sls offline
```

To run the single handler():

```
sls invoke local -f api
```

```
yarn && yarn start
```

`http://localhost:8000`
